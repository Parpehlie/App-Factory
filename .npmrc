# React Native / Expo require a hoisted node_modules layout so Metro and native
# autolinking can resolve packages. See https://docs.expo.dev/guides/monorepos/
node-linker=hoisted

# PostHog and a few RN libs declare many optional peers; do not block install on them.
strict-peer-dependencies=false
auto-install-peers=true
